services:
  server:
    build:
      context: .
      dockerfile: dev.Dockerfile
    image: phonebook-back-dev
    container_name: phonebook-back-dev
    volumes:
      - ./:/usr/src/app
    ports:
      - 3001:3001
    env_file:
      - .env
